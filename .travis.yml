language: c
os: osx

branches:
  only:
    master

install:
  - brew install snaipe/soft/criterion

script:
  - ./test/coretest --verbose

notification:
  slack:
    on_succes: always
    on_failure: always
